<header class="glass-header">
  <div class="container header-content">
    <div class="logo">
      <h1 class="gold-text">{{ shop.name }}</h1>
    </div>
    <nav>
      <ul class="nav-links">
        <li><a href="/">الرئيسية</a></li>
        <li><a href="/collections/all">المنتجات</a></li>
      </ul>
    </nav>
  </div>
</header>

<style>
.glass-header {
  padding: 20px 0;
  background: var(--glass);
  backdrop-filter: blur(15px);
  border-bottom: 1px solid var(--border-glass);
  position: sticky;
  top: 0;
  z-index: 1000;
}
.header-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.nav-links {
  list-style: none;
  display: flex;
  gap: 20px;
}
.nav-links a {
  color: white;
  text-decoration: none;
  font-weight: bold;
}
</style>
